package GameLoop;
import Levels.TileSet;

import java.awt.*;

public class LevelRenderer {

    private TileSet tileset;

    public LevelRenderer(TileSet tileset) {
        this.tileset = tileset;
    }

    private boolean generateView(TileSet tileset) {
        /*TODO
        Generate view from provided tileset (based on x value, decide which tiles to display)
        Type boolean is temporary while I figure out what type of Object should be returned for rendering
         */
        return false;
    }
}
